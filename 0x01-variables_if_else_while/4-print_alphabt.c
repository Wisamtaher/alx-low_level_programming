#include <stdio.h>

/**
 * main - enty point
 * Description: Print all the letters except q and e
 * Return: zero
 */

int main(void)
{
	int n = 97;

	while (n <= 122)
	{
		if (n == 101 || n == 113)
		{
			n++;
			continue;
		}
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}

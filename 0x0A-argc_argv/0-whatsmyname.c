#include "main.h"

/**
 * main - function
 * @argc: argument number
 * @argv: argument array
 * Return: zero
 */

int main(int argc, char *argv[])
{
	if (argc >= 1)
	{
		printf("%s\n", argv[0]);
	}

	return (0);
}
